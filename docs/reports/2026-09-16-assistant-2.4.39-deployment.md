# Assistant2.4.39 — exact runtime deployment

Final checkpoint: **2026-09-16 06:59:32UTC**. Lifecycle: **production-verified**.

## Source and production identity

- Exact deployed source: `2c72e01cb28452c640c033c91a2060a8eca57201`.
- Public Assistant release: **2.4.39 / 2026-09-16**; rendered footer
  `Версия 2.4.39 от 16.09.2026` verified inside the exact image with an offline
  fake Telegram transport, not inferred from the image tag.
- Git archive SHA-256:
  `d8a84d25e5e54c0c6120da59a77380c956d873f531ea452dfd8fa5f7743a2d89`.
- Runtime image:
  `sha256:7fc9a7e61fde4efeca2bc5c3f28a91b6078cb6aadaad0ce1f09a4f505ab36748`.
- Container:
  `25f5cc362f08760b944aecc1c0b2f95e0de1f8ec9d77d10559572f2ad77144af`.
- StartedAt: **2026-09-16T06:57:40.921400883Z**; healthy, restart count0.
- Integrated into main by `762c251ff0fc96bdb6e834ccab4247bdcba0c801`, then
  pushed. Direct GitHub ref verification matched both main and candidate branch.
  Runtime/core/infra trees match C exactly; later docs commits are not runtime
  image identities. The candidate was built from its Git archive, no overlay.

## Approval, scope and technical decisions

PO root request: «Ну так задеплой и запуш». After the legacy/downgrade gates
were presented, task «Ассистент» relayed the explicit follow-up:
«Какие воросы? О чем ты? Просто деплой. Зачем все это? Все вопросы решай сам.»
Root accepted the remaining technical decisions and deployed only this release.

Lease `ask-protocol-2c72e01-20260916`, one root writer/key-only master on
`news-vps`, named only `aichattg-telegram-runtime`; issued06:53UTC, expiry07:50UTC.
One recreation, zero emergency stops, zero binary rollbacks or data restores.
Server lease closed06:59:11UTC; master exit and socket absence verified by
06:59:32UTC. The initial active lease JSON is historical issuance evidence;
closure supersedes it. No current production or transport authority remains.

The accepted conservative transition quarantines late deliveries **and edits
of pre-upgrade native messages**. It does not pretend to preserve moderation of
those old edited messages. New native messages use the new protocol. A single
proved-safe legacy job may finish original work; conflicts/unknown calls are
not replayed. Legacy tables contain identifiers only, not imported message text.

The neutral invalid-judgement fallback is:
«Сейчас не удалось обработать вопрос. Попробуйте, пожалуйста, позже.»
It is an operational response, not an unjudged substantive answer or sanction.
The hint includes the PO-requested30-second sentence; this is idle UI cleanup,
not a deadline after which a late question becomes invalid.

## Shipped behavior

- One durable native-message/revision judge owner: Assistant for addressed
  questions, Moderator for ordinary posts. Strict bound semantic results and
  code-derived sanctions; opaque generation/lease fences prevent stale actions.
- Native-once Assistant delivery, including edits, async preflight, multipart,
  fallback, uncertain outcomes and preserved provider-usage accounting.
- Independent30-second idle `/ask` command/hint cleanup; authenticated reply
  cancels expiry before model work. Delayed ACK, restart, early/late reply and
  cleanup race regressions retain substantive questions/answers.
- Existing provider/model tuples, quotas, policy thresholds, chat scope,
  approved domain bodies and knowledge data remain unchanged. The preceding
  suspected porn-spam policy remains included.

## Evidence

- `passed`: root full suite again after main integration — **1146 total,
  1141 passed,0 failed,5 unchanged fixture skips**. Migration/import9/9,
  Gatekeeper scenario30machine/34human, isolation and whitespace gates passed.
- `passed`: exact clean C source guard versus productiona41518f. An initial
  invocation from merge HEAD correctly rejected its SHA mismatch; the exact
  candidate worktree was then used and passed, not a bypass of the guard.
- `passed`: independent source reviews — pure81/81, timer68/68 plus4 probes,
  answer55/55 plus5 probes, forward transition10/10; overlapping review counts,
  not additional full-suite totals.
- `passed`: exact old→new schema rehearsal, unchanged53 old objects,25 additions
  (10 tables,3 named indexes,12 automatic indexes), restart idempotence.
- `passed`:22/22 offline release-helper tests. Review found that a five-minute
  activation reserve could expire before emergency STOP; root increased
  admission reserve to20minutes. A near-worst-case admitted failure completes
  STOP before expiry. Short lease, old/foreign image, Console drift, repeated or
  uncertain STOP all fail closed. No production fault injection was performed.
- `passed`: fresh baseline06:54:53UTC matcheda41518f/2.4.38, image dd64eee0,
  healthy/restart0 and untouched Console3.2.0/f650fe8. Exact env/config and source
  gates ran again before activation; build and helper/archive hashes matched.
- `passed`: production checks06:57:48,06:58:11 and06:59:11UTC:64 runtime/core
  package files matched, exact footer rendered, local health200, restart0,
  unchanged runtime env/mounts/routes/log rotation and Console snapshot.
- `passed`: all53 historical production schema objects unchanged, exact25
  additions. Compare against the live baseline, not fresh-schema formatting.
  Integrity/FK, unseeded historical coordinates, unmapped jobs, legacy/protocol
  overlap and head consistency: all eight invariant counts0.
- Final aggregates:363 legacy native coordinates,426 legacy jobs,56 conflicting
  legacy coordinates quarantined;1 new protocol envelope/head observed.
  This is not a semantic-quality or live answer/cleanup acceptance test.
- `passed`: bounded public HTTPS checks: health200, unauthenticated Console
  release401 and both webhook routes401; no authenticated event injection.
- `failed`: unrestricted old-a41518f binary downgrade safety, as already proven
  locally. That image must not be restarted against this new protocol database.
- `not_run`: new paid evaluation, real Telegram question/cleanup/sanction test,
  private Review activation, or knowledge-answer quality repair.

## Preservation and recovery

Runtime env digest remains
`cb62caeb5561f4b3dbfeb5b5c0966f89fb22097a1a5333a59fc73793a12b08a6`;
private env-file digest remains
`d1d39d33185fc2544c7e68c6368b1ff1ae36548a9017c79b0b7aa295a0d9fc88`.
New schema digest:
`fa84bf7ac3a19a1c9492d5d8c714b93705fa5452de26ea18dfec30dc29dfa541`.
No raw private data, configuration values or Docker logs were exported.

Console remains3.2.0, source `f650fe87cabf5cf498a64d48907f7a924861cfd2`,
image `sha256:6f76808829b1c0e4bc2544330f23fadd3f529e2991e98ce3bd1cac6a811af1e9`,
same container/start/config/mounts/routes. No Console, private Review, Gatekeeper,
News, host logging, secrets or webhook configuration mutation occurred.

Recovery is **stop the exact new runtime, preserve data, then tested forward
repair**, under a fresh exact authority now that this lease is closed. Do not
apply old runbook image rollbacks335a35a/a41518f or restore a database blindly.

Separate residuals: the historical rejected-router field remains unidentified;
2.4.39 supplies validated ownership and a non-silent operational fallback, not
proof that the provider always returns valid results. The reported
`missingDomains: content` answer-quality issue and private Review choices remain
outside this release. No claim that all backlog items are deployed.

## Local evidence

Ignored artifact directory: `output/ask-protocol-release-v1/`. No raw transcript
or private database belongs in Git. SHA-256:

- `final-verification.json`:
  `fc9f84d88f1bcd88a19ad65fce6923a1d8c80ca87da663428211132361ef2e5f`.
- `closure-server.json`:
  `a26089f4cfa08d3cd5b5342a4710cd225c90f510a0a3eeaef84f4f5949b1487c`.
- `public-check.json`:
  `571abbfdb23697bba627a787ca7efb453222d7fea8d36ba2c5ef5095578be99a`.
- Reviewed deployed `release.py`:
  `27f5ed379b1f855f0449465e87f158a833876adb75621610836e9eb130f78af2`.
- `expected-schema.json`:
  `149a6e8b6658c73e94bdedb36f9c4ae1543d3a1ccd640b3a9614c71ea47095f5`.

`closure.json` adds the local master-exit/socket-absence proof to the server
closure, whose transport-close field necessarily precedes the local SSH exit.
