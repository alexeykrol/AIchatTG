# Assistant 2.4.37 deployment — 2026-09-15 UTC

Lifecycle: `production-verified`.

## Scope and authority

The Product Owner explicitly requested «Исправь, закомить, запуш и задеплой
это» after reminding the integrator of the version/date footer requirement.
The integrator announced the restored Assistant component line as `2.4.37`
after historical candidate `2.4.36`, separate from project/npm versions.
The one-time `assistant-version-2.4.37-20260915` lease expires 21:30 UTC;
one repository-root integrator owns the single key-only `news-vps` SSH master.

Only `aichattg-telegram-runtime` was built and recreated with
`--no-build --no-deps`. No config/secrets, webhook, bot settings, paid-model
policy, database/schema, knowledge, Console, Gatekeeper or News changes were
authorized or performed. No new paid test or Telegram message was sent.

## Exact release

- Assistant metadata: version `2.4.37`, release date `2026-09-15`.
- Visible footer: `Версия 2.4.37 от 15.09.2026`.
- Production source: `335a35ad344706062a292581db4d27c5776c4302`.
- Prior production / rollback: `0b541483ac1c8f2eee32127240c7a0b9f38d4466`.
- Git archive SHA-256:
  `4a921a3a541b3e7fd1aed0a1bded53223279c424e481450a02d10b311ee0f73c`.
- Image ID:
  `sha256:6c94e20ead5b17f4d50c82447d65d20f70d180df0ec8e222d29e8a73669a8b30`.
- Container ID:
  `2ced0e9713f5ad49fa156b7f0ef8e29ee38654b006a2dbccf652349ea95e60b8`.
- Runtime started: `2026-09-15T21:01:48.806052837Z`.

The source was verified in a clean detached worktree against exact previous
production, pushed to canonical `origin/main` and archived directly from Git.
There was no overlay. The release gate reports metadata restoration as
bootstrap (`comparison: not_run`), because previous production had no component
metadata; structural validation passed. It does not invent a historical version.

## Delivered contract

Canonical `assistant-release.json` supplies the footer through the shared
Assistant send path, after Markdown rendering. The transport reserves space,
preserves emoji boundaries, handles plain fallback and places the full footer
once on the final answer part. Approved body copy, forceReply, first-part
receipts and no-retry delivery fences remain intact. No new responses were
introduced for previously silent cooldown, quota or rejection paths.

Bounded dialogue memory retains only the answer body. Durable answer records
and local visible transcripts include the logical delivered footer. Current
dual-dialogue tests were adjusted for the existing whitespace normalization of
multiline replies; raw ledger hashes and durable-record equality checks remain.
Historical frozen sources, gold, paid captures and leases were not changed.

Future Assistant-affecting source releases must increase the public component
version and use a non-backward valid release date. The guard reads committed
JSON as data rather than executing candidate code. Root tests, Codex/Claude
instructions and the cutover runbook now share this gate.

## Verification

`passed`:

- Root suite 758/758 top-level tests, zero failures/skips: Gatekeeper132,
  runtime416, core114, snapshot4, infra2, release guard61, log safety4,
  acceptance-runner17, Console8.
- Nested default historical lane:23 passed /6 explicitly skipped local-evidence
  checks. Separate original-receipt lane:29/29 passed without new network calls.
- Migration safety:9/9; no production migration. Scenario30 machine/34 human
  entries, isolation, whitespace checks and independent offline review passed.
- Runtime tests exercise seven delivered reply paths through a fake HTTP
  transport, duplicate suppression, body-only memory and durable answer text.
- Read-only production inspection at20:59:31 UTC confirmed exact prior image,
  healthy/restart0 and SQLite `quick_check=ok`. Build preflight at21:01:26 UTC
  confirmed the same state; all64 rendered service environment values, routes
  and mounts were preserved.
- Post-deploy verification at21:02:15 UTC: exact image/revision; all57 deployed
  runtime/core files match the archive; healthy/restart0, internal health200,
  SQLite `quick_check=ok`; unchanged environment, schema, mounts, routes,
  logging configuration and private env-file digest.
- Four offline fake-transport cases inside the actual image: approved three-
  paragraph body, HTML reply, long text and emoji-heavy text. Part counts
  `1,1,3,3`; exact footer once on the final part; body and first reply target
  preserved. No real Telegram/provider calls were made by these checks.
- Console retained the same container/image, healthy/restart0; prior runtime
  image retained and exact rollback digest verified.
- Public HTTPS checks: health200; unauthenticated Assistant and Moderator
  webhook requests401. No secret or valid Telegram update was submitted.
- Repeat verification at21:04:31 UTC, 136 seconds after the first check:
  same container/image/start time, healthy/restart0; all57 source hashes and
  four offline footer cases passed again; env/schema/routes/mounts/logging/
  Console unchanged; rollback image still present.

`failed`: none in the final local suite or either production verification.

`not_run`: new paid model tests, real Telegram/footer/menu/client acceptance,
webhook or bot-setting changes, migrations/imports, Docker-log reads. Earlier
ordinary-user menu cleanup and answer-quality acceptance gaps remain open;
this release does not claim to resolve or retest them.

## Rollback and retained evidence

Retained prior image:
`sha256:de7d21b63d234b501483d0fa70381336e631e4ede4fb2649fd8f08744f658565`.
Rollback recreates only runtime from the exact `0b54148` release/image,
preserving the current live database and unchanged environment. No database
restoration is authorized or needed.

Content-free receipts are retained locally in ignored
`output/assistant-version-2.4.37/` and on the host in
`/home/agent/aichattg/releases/335a35ad344706062a292581db4d27c5776c4302/evidence/`.
Earlier version audit and deployment reports remain preserved.
The one-time lease was consumed and closed at21:04:39 UTC; the only SSH master
was closed and control-socket absence verified. Rollback was not needed.
