# Assistant 2.4.41 — verified runtime-only deployment

Lifecycle: `production-verified`.
Public version/date: **2.4.41 / 2026-09-19**.

## Identity and authority

- Exact deployed source: `e52348537f0445f285487a957e0b43f47bd0018d`.
- Image: `sha256:dc7585b68a99929c0ce88314fe71f86b46b3b24ef2973c8bdcd8c7df63416bf5`.
- Runtime container: `4ee62b9ef43d95740605f244d2250960b511fdc7d3455f27108b29451e664505`.
- Started: **2026-09-19T22:06:09.298225502Z**; healthy, restart count 0.
- Previous source: `2c72e01cb28452c640c033c91a2060a8eca57201` / Assistant 2.4.39.
- Root owner: `019fd019-af89-7b50-a16d-8c7928753f24`, sole `news-vps` master.
- PO explicitly requested find/fix/deploy of the irrelevant-question failure;
  exact source, baseline, effects and rollback were reported before mutation.
- One-time lease 22:04–22:49 UTC: one runtime activation, at most one rollback
  to the exact 2.4.39 image, no database restore or other production changes.
  Consumed and closed **22:07:05.184319 UTC**; one activation, zero rollbacks.
  SSH master closed **22:07:05 UTC**, socket absence verified by wrapper.
- Candidate branch and main integration `4847a68` were pushed and remote refs
  checked. Main also retains pending Review code; **main was not deployed**.

## What changed

Strict closed JSON schemas for the two existing safety calls and explicit
clean-target/context instructions now match the unchanged semantic validator.
Incomplete/refused/malformed responses still fail closed without retries.
The existing terminal judgement result stores allowlisted reason/completion
metadata, never raw provider text or prompts. Counters describe only the failed
stage, not full two-stage cost. Approved irrelevant-question/help bodies,
domain routing, knowledge and moderation sanctions were not rewritten.

The confirmed historical failure was safety contract rejection before content
routing. Which field the historical model actually returned remains unknown.
The clean `target=assistant` inconsistency is a locally reproduced ambiguity,
not a recovered historical response. See the immutable
[diagnosis](2026-09-19-assistant-reply-incident.md) and
[candidate acceptance](2026-09-19-assistant-safety-contract-candidate.md).

## Verification

- `passed`: exact clean-source/version guard versus live 2c72e01;
  isolated release suite **1164 passed / 5 explicit skips / 0 failed**;
  migration fixtures **9/9**; Gatekeeper scenario 30 machine/34 human entries;
  isolation and whitespace checks.
- `passed`: main integration suite **1450 passed / 5 skips / 0 failed**.
  This includes pending Review code and is not a claim that Review shipped.
- `passed`: independent **4636** parser differential cases and **11** offline
  release-helper scenarios, including absent-container rollback, stale lease,
  unknown image rejection and terminal rollback closure.
- `passed`: both native webhook orders, repeated help, raw suffix ask,
  invalid safety then new native question, duplicate/restart once-only behavior,
  real-runtime domain-no-signal copy/footer, threat and abuse gating, sanitized
  diagnostic persistence and stale lease fencing, using fake transports.
- `passed`: **64 deployed source hashes** match the exact archive;
  image label matches e523485; rendered footer in that image is
  `Версия 2.4.41 от 19.09.2026`, checked with zero external sends.
- `passed`: repeated runtime health/restart 0; database quick check `ok`,
  unchanged schema digest
  `fa84bf7ac3a19a1c9492d5d8c714b93705fa5452de26ea18dfec30dc29dfa541`.
- `passed`: environment file bytes, running environment, mounts, routes and
  bounded json-file logging preserved. No migration, webhook/config/secret
  change, data import/restore, knowledge change or Docker-log read.
- `passed`: HTTPS health 200; unauthenticated operator API and both webhook
  routes reject with 401. Both read-only `getWebhookInfo` checks at22:06:48UTC
  have pending 0, correct routes and no last error.
- `passed`: Console remains **3.2.0 / f650fe8**, image6f768088, exact same
  container f2212012/start time2026-09-17T11:48:29.86615973Z, healthy/restart 0.
- `not_run`: new paid model calls, synthetic Telegram questions, live-model
  schema/classification acceptance, sanctions and actual rollback execution.
- `inconclusive`: exact invalid historical field and the later normal reply
  mentioned by the owner; bounded retained records did not establish it.

## Preserved queue and rollback

Private Review7d99ae0/Assistant2.4.40 was **not** included or activated. Its
retained empty store/config is untouched. The old same-SHA retry is now stale:
it would remove this repair. Any Review activation needs a new candidate that
includes this source, advances Assistant metadata beyond 2.4.41, passes the
release guard and receives fresh exact PO approval/lease.

Baseline 2.4.39 image7fc9a7e6/source/config remain available. Same-schema rollback
would retain all data and cannot undo replies or live sanctions; no rollback
to older pre-ask-protocol binaries is authorized. The consumed lease authorizes
no further mutation.

Ignored local receipts: `output/safety-routing-release-20260919/`.
SHA-256:

- exact archive: `bca08b7cd4f518bf27ceada1581a24530b867f001cab4020688ba6824c04dd79`;
- final verification: `9b21580832f36efeb3c4f2952e38f96eb498cd4fa8c50a9d59eb017b07627444`;
- closure: `cd943967e9bf1d8c46e1e8df3bf16f4097b3dd4ee4c35640052beb35b0b43b43`;
- public boundary: `0a308eefff3ebffd0db40a35e7a1b8a634ba367b9b7b9362eb50d1b3d0bfa4c3`;
- webhook status: `ab4205df2f77b3973f0da7de53a7b20df84dfe2762848fc39d8d2a3109396b11`.

No raw chat transcripts, credentials, database or private provider output were
committed. Available local dialogs were archived outside Git.
